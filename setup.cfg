[metadata]
name = automata.data.stream
version = 0.0.1
description = Automata Data Stream
license = UNLICENSE

[options]
packages = find:
install_requires =
    automata.core=0.0.3
    websockets==10.2

[options.packages.find]
include = data*
exclude =
    tests
    simulations
